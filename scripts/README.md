# Scripts

This directory intentionally contains no LFS build script yet. Import existing VM scripts only after sanitization and review, following the [import guide](../docs/pt-BR/importar-scripts-vm.md).

Do not infer a script exists because a document mentions a planned automation.
