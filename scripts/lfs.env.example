# Copy to a private, untracked file only after VM audit.
# Do not commit actual paths, hostnames, credentials or unreviewed values.
EXPECTED_LFS=/mnt/lfs
EXPECTED_LFS_TGT=x86_64-lfs-linux-gnu
# Set only after confirming CPU count and the build plan:
# EXPECTED_MAKEFLAGS=-j4
