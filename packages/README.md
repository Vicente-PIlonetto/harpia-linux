# Package recipes

Recipes and repositories are planned. They must identify source, version, hashes, signature status, build dependencies, license and maintainer review.

The included template is documentation only and is not executable.
